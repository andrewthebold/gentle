include: package:lint/analysis_options.yaml

analyzer:
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false

linter:
  rules:
    # Off for [freezed]'s generated files
    sort_unnamed_constructors_first: false
